String b;


void setup(){
  pinMode(0, OUTPUT);
  pinMode(0, INPUT_PULLUP);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  Serial.begin(9600);
  for(int i = 0; i < 10; i++){
    for(int i = 0; i < 10; i++){
      for(int i = 0; i < 10; i++){
        for(int i = 0; i < 10; i++){
        }
      }
    }
  }
  b.concat(String("Block A"));
}
