void setup(){
  pinMode(0, INPUT);
  pinMode(0 + 0, INPUT);
}

void setup(){
  pinMode(0 + 0, INPUT);
  pinMode(0, INPUT);
}

void loop(){
}

void setup(){
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}

void setup(){
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}
