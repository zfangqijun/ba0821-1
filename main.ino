// 定义 124
void my_124(){
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}

// 定义 123
void my_123(){
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}

// 定义 125
void my_125(){
  pinMode(0, INPUT);
}


void loop(){
  while(false){
    while(false){
      while(false){
        while(false){
          while(false){
            while(false){
              while((false && ((false && ((false && false)))))){
              }
            }
          }
        }
      }
    }
  }
}

void setup(){
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}

void setup(){
  while(false){
  }
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}

void setup(){
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
  pinMode(0, INPUT);
}
